# Task2

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 20.3.25.

## Angular CLI latest version
npm install -g @angular/cli

## Angular CLI specific version
npm install -g @angular/cli@17 global



## create new project with latest version
cmd
ng new project-name
cd my-app
code.

## create new project with specific version
cmd
npx @angular/cli@16 new my-app
cd my-app
code.



## Development server 

To start a local development server, run:
```bash
ng serve
ng s -o   //to open run direct in browser
```



## generate new component

Angular CLI includes powerful code scaffolding tools. To generate a new component, run:

```bash
ng generate component component-name
ng g c componentname
ng g c cname --inline-style // not depend on css
ng g c cname --skip-tests  //ignoe tisting
```








